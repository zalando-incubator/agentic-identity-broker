{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agentic-identity-broker:events:v1:approval-requested",
  "title": "approval-requested",
  "description": "A new pending approval is created.",
  "allOf": [
    {
      "$ref": "urn:agentic-identity-broker:events:v1:envelope"
    },
    {
      "properties": {
        "type": {
          "const": "agentic-identity-broker.approval-requested"
        },
        "outcome": {
          "const": "pending"
        },
        "data": {
          "type": "object",
          "additionalProperties": false,
          "properties": {}
        },
        "subject": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "approval_id",
        "agent_id"
      ]
    }
  ]
}
