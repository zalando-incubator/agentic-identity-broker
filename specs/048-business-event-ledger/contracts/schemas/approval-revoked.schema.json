{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agentic-identity-broker:events:v1:approval-revoked",
  "title": "approval-revoked",
  "description": "An existing approval is revoked.",
  "allOf": [
    {
      "$ref": "urn:agentic-identity-broker:events:v1:envelope"
    },
    {
      "properties": {
        "type": {
          "const": "agentic-identity-broker.approval-revoked"
        },
        "outcome": {
          "const": "success"
        },
        "data": {
          "type": "object",
          "additionalProperties": false,
          "properties": {}
        },
        "subject": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "approval_id",
        "agent_id"
      ]
    }
  ]
}
